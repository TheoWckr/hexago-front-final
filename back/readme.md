# Back end of Hexago
ide : Webstorm

# Prerequisites

run:
npm install

to launch the server, use :
DEBUG=backend:* npm start

#Apidoc

install api doc, run:
npm install apidoc -g

to run api doc:
apidoc -i routes/ -o apidoc/